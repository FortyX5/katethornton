# Kate Thornton — Personal Creative Hub

Static one-page site for GitHub Pages.

## Publishing

Upload `index.html`, `styles.css`, and the complete `assets` folder to the repository root. In the repository’s **Settings → Pages**, choose **Deploy from a branch**, then select **main** and **/(root)**.

The site contains no build process or dependencies.
